name: "olist_analytics"

version: "1.0.0"

config-version: 2

profile: "olist_dbt"

model-paths:
  - "models"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_analytics:

    staging:
      +materialized: view
      +schema: STAGING

    marts:
      +materialized: table
      +schema: ANALYTICS